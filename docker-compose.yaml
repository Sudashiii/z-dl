version: '3.8'

services:
  zlibrary-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: zlibrary-app
    container_name: zlibrary-app
    ports:
      - "6126:6126"
    volumes:
      - ./z-dl:/app
    environment:
      - FLASK_ENV=production